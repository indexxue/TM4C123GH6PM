---
description: TM4C123 四轴小车项目上下文 — 每次对话必读
alwaysApply: true
---

# TM4C123 四轴小车项目上下文

## 项目是什么

- **目标**：基于 **TM4C123GH6PM**（Cortex-M4F @ 80 MHz）的 **FreeRTOS** 四轴 MG513 电机小车底盘固件
- **硬件**：自定义 PCB，**8 MHz** 主晶振 + PLL → 80 MHz 系统时钟；引脚以 `projects/<car>/.syscfg/` 为准（电机、编码器、蓝牙、IMU、磁力计、OLED、ADC 循迹/按键/电池、超声波等）
- **参考**：EK-TM4C123GXL LaunchPad 仅作早期对照（LaunchPad 为 16 MHz 晶振，**本仓库固件统一按 8 MHz 配置**）
- **仓库**：https://github.com/indexxue/TM4C123GH6PM

## 命名规范

改文件、加脚本、写文档时遵循下表；**禁止使用**已废弃名称（如根目录 `compile_commands.json`、`pinout.c`）。

| 类别 | 规则 | 示例 |
|------|------|------|
| 编译产物 | `projects/<car>/build/<car>.{elf,bin,map}` | `projects/car-4wd/build/car-4wd.elf` |
| IDE 编译数据库 | `projects/<car>/build/ide-compile-db.json` | 勿用 `compile_commands.json` |
| 板级生成代码 | `projects/<car>/board/src/*.c` + `board/inc/*.h` | `motor.c`、`encoder.c`、`line.c`、`board.c` |
| 板级配置源 | `projects/<car>/.syscfg/` | `gpio.json`、`modules/motor.json` |
| 链接脚本 | `ld/<用途>.ld` | `app.ld`、`bootloader.ld` |
| 设计文档 | `docs/<主题>.md`（kebab-case 英文） | `build.md`、`syscfg-io-allocation.md` |
| 脚本 | `scripts/<动词_名词>.ps1` 或 `<名词>.py` | `build.ps1`、`gen_config.py`、`flash-uniflash.ps1` |
| 根目录快捷方式 | `<动作>.cmd` 包装对应 ps1 | `build.cmd`、`flash.cmd` |
| C 源文件 | `snake_case.c`；模块名简短表意 | `factory.c`、`nvs.c` |
| BSP 驱动 | `bsp_driver/{inc,src}/bsp_<外设>.{h,c}` 成对；禁止裸名 `uart.h`/`clock.h` | `bsp_uart.c`、`bsp_timer.c`、`bsp_sysctl.c` |
| BSP 头 guard | `BSP_DRIVER_<外设>_H` 大写 | `BSP_DRIVER_UART_H` |
| 头文件 guard | `MODULE_<名>_H` 或功能前缀 | `MODULE_MOTOR_H` |

## 技术栈

| 组件 | 路径/版本 |
|------|-----------|
| 语言 | C11，4 空格缩进 |
| SDK | TivaWare C Series 2.2.0.295（`sdk/TivaWare_C_Series-2.2.0.295`，首次构建自动安装） |
| RTOS | FreeRTOS v8.2.3（`sdk/.../third_party/FreeRTOS`，GCC ARM_CM4F 移植） |
| 外设 API | TivaWare DriverLib（`driverlib/*.h`） |
| 板级配置 | `.syscfg/*.json` → `gen_config.py` |
| 工具链 | arm-none-eabi-gcc（首次构建自动下载到 `tools/`） |
| IDE | Cursor / VS Code，`Ctrl+Shift+B` 触发编译 |

## 目录结构

```
bsp_driver/             # MCU 外设薄封装（bsp_<外设>.{h,c}，见 embedded-coding-standard.md）
cbb/                    # 芯片驱动子模块（ws2812b、mpu6050…）
projects/
  car-4wd/              # 四轮（.syscfg、board/ 设备库、main/ 应用、build/）
  car-2wd/              # 两轮
bootloader/             # Bootloader
factory/                # 厂测固件
Common/src|inc/         # 跨工程公共模块（log、start、device_profile、nvs、ota）
include/                # 全局头文件（start.h、FreeRTOSConfig.h）
sdk/                    # TivaWare SDK（首次构建安装，不提交）
scripts/                # build.ps1、gen_config.py、flash-jlink.ps1
ld/                     # 链接脚本
docs/                   # 设计文档
```

## 构建与配置

完整说明见 **`docs/build.md`**。

```powershell
.\build.cmd                              # 默认 car-4wd
.\build.cmd -CarProject car-2wd
.\flash-jlink.cmd
```

- 配置源：`projects/<car>/.syscfg/`（**勿手改** `projects/<car>/board/src/{motor,encoder,line,board}.c`）
- 生成：`projects/<car>/board/src/motor.c`、`encoder.c`、`line.c`、`board.c`
- 初始化：`projects/<car>/main/main.c` 调用 `Start_Init()`（`Common/src/start.c`）

## 外设分配摘要

以 `projects/car-4wd/.syscfg/` 为源；下表为 **car-4wd** 摘要。

| 功能 | 接口 | 引脚/模块 |
|------|------|-----------|
| 4 路电机 PWM | Timer0/1 | PB6/PB7, PB4/PB5 @ 10 kHz |
| 方向控制 | GPIO | PF4/PC7 (M1), PC4/PA6 (M2), PA7/PF0 (M3), PF1/PF2 (M4) |
| 编码器 | M1/M2 **硬件 QEI**（PC5/6、PD6/7）；M3/M4 **软件 QEI**（PF3/PD4、PD5/PB0） | 板级已去 RC 滤波，M1/M2 走硬件正交解码 |
| 蓝牙 | UART0 | PA0/PA1 @ 115200 — **协议层**（遥控/上位机通信，非 CMD） |
| 调试串口 | UART7 | PE0 RX / PE1 TX @ 115200 — **日志 + CMD 命令行** |
| I2C 总线 | I2C0 软件（GPIO 位操作） | PB2/PB3 — MPU6050@0x69、QMC5883P@0x2C（暂无 OLED） |
| SPI / SSI0 | SSI0 | PA2~PA5 — 预留 SPI 外设 |
| 电池电压 | ADC1 SS2 AIN0 | PE3 |
| 按键 | ADC1 SS0 AIN1 | PE2（电阻分压采样） |
| 循迹 | **ADC0 SS0** AIN4~9 | 左→右 **PD3/PD2/PD1/PD0/PE5/PE4**（**6 路**）；**PD1/PD0 为中线** |
| 超声波 | GPIO | PC2 Trig, PC1 Echo |
| 蜂鸣器 | GPIO | PB1 |
| RGB LED | GPIO | PC3 (WS2812) |

完整引脚表见 `docs/syscfg-io-allocation.md` 与编译生成的 `projects/<car>/gpio-allocation.md`。

### ADC 分工（car-4wd）

TM4C123 **仅有 ADC0 + ADC1**（无 ADC2）。同一 AIN 引脚可接任一模块，但固件按模块拆分，避免 ADC1 多序列竞争：

| 模块 | 序列 | 通道 | 用途 | 板级配置 |
|------|------|------|------|----------|
| ADC1 | SS0 | AIN1 | 按键 | `button.c` 内 `s_button_adc_cfg` |
| ADC1 | SS2 | AIN0 | 电池 | `BOARD_BATTERY_ADC_CFG` |
| **ADC0** | **SS0** | AIN4~9 | 循迹 ×6 | `BOARD_LINE_ADC_CFG` + `Line_Sample()` |

- 配置源：`projects/car-4wd/.syscfg/modules/adc.json`（循迹项 `module: ADC0`）
- 生成代码：`board.c`（`BOARD_LINE_ADC_CFG`）、`line.c`（`Line_Init` / `Line_Sample` / `Line_IsReady`）
- 初始化顺序：`Board_Periph_Init()`（引脚 mux + ADC init）→ `Line_Init()`（`start.c`，须在 mux 之后）
- 应用层：`app.c` 周期日志 `app: line adc=[...]` / `app: line det=[...]`（编码器周期日志默认 `APP_ENC_LOG_ENABLE=0` 关闭）
- 协议：**姿态+编码器常驻推送**；电量/循迹/超声/RPM 按需 `SUBSCRIBE`（上位机仪表盘勾选）


## 串口与通信分工

两路 UART **职责分离**，新增功能时不得混用：

| 接口 | 用途 | 模块/约定 |
|------|------|-----------|
| **UART7**（调试） | 开发日志、`cmd` 命令行（`ftmenter`、`motor`、`adc` 等） | `log`、`cmd`（`cmd_uart_line_service_start`） |
| **UART0**（蓝牙） | 协议层：遥控、上位机/App 二进制或帧协议 | 独立协议栈；**禁止**将 `cmd` 读行任务绑到 UART0 |

- 产线调试、厂测、固件开发：接 **UART7**（PE1 TX @ 115200）。
- 蓝牙 HC-05 仅承载业务协议，不与 `cmd` 共用同一物理口。

## 当前开发状态

平台层：量产 app UART7 日志；厂测 factory UART7 日志 + CMD；UART0 蓝牙协议层已接入；板级外设 init 已接通。

**已接通**：IMU/磁力计（I2C0 软件 I2C）；**ADC 循迹 6 路采样与阈值检测**；**循迹外环**（`line_follow` + `SET_LINE_FOLLOW`）；**编码器计数与差分测速**（M1/M2 硬件 QEI + M3/M4 软件 QEI，`motion.c` + `chassis.c` 速度环）。

**待实现**：按键业务逻辑（模式切换）。

## 开发约束

- **串口分工**：`cmd` 走 **UART7**；**UART0** 仅协议层，勿把调试/厂测命令接到蓝牙口
- **I2C0 必须软件 I2C**：PB2/PB3 仅用 GPIO 开漏位操作；**禁止**对 I2C0 调用 `I2CMasterDisable/Enable` 等硬件寄存器（外设时钟未开时会卡死）
- **不要提交**：`build/`（含 `ide-compile-db.json`）、`tools/`、`downloads/`、`sdk/`（除 `sdk/README.md`）
- **不要手改** `projects/*/board/src/{motor,encoder,line,board}.c`（改对应 `.syscfg` 后重新编译）
- **循迹 ADC**：走 **ADC0** 独立序列，勿与电池/按键共用 ADC1 多序列混采；无 ADC2
- 提交前本地编译通过；提交信息简短中文/英文，说明做了什么/为什么
- `build.ps1` 使用 `-mfloat-abi=hard` + FPU（以 PowerShell 脚本为准）

## 编码器硬件说明（car-4wd）

- **板级**：编码器 A/B 相 **已无 RC 滤波**（原 RC 会钝化边沿导致硬件 QEI 不计数）。
- **M1/M2**：**硬件 QEI**（M1→QEI1/PC5+PC6，M2→QEI0/PD6+PD7；PD7 须 `GPIOUnlockPin`）。
- **M3/M4**：**`bsp_sw_qei`**（GPIO 边沿中断 + 4x 正交表）；`app_tmr` 1 ms 轮询 `bsp_sw_qei_poll_all` 作备份。
- **配置源**：`projects/car-4wd/.syscfg/modules/encoder.json`（`interface: qei` / `gpio`），**勿手改** `encoder.c`。
- **验证**：手转电机时 `Encoder_GetCount(0..3)` 应对应 M1..M4 变化；上位机订阅 **ENCODER** 通道或 GET_TELEMETRY 看计数。。

## 厂测启动卡死排障（高频）

### 典型现象

UART7 只看到：

```
boot B
[factory] boot
```

之后无任何 `[INFO]` 日志，表现为“程序卡死”。此问题在厂测/I2C 改动后多次出现。

### 正常启动日志阶梯（UART7 @ 115200）

按顺序应出现（`bsp_uart_debug_puts` 阶段）：

```
boot B
[factory] boot
[factory] i2c sw init
[factory] board ok
[factory] board init done
[factory] nvs init
[factory] tasks start
[factory] rtos start
```

调度器起来后（`LOG_INFO` 阶段）：

```
[INFO] factory:nvs serial=...
[INFO] factory:i2c idle scl=1 sda=1 (1=high)
[INFO] factory:i2c scan 0x2C,0x69 (PB2/PB3 I2C0)
[INFO] factory:imu ready ...
[INFO] factory:mag ready ...
```

**卡在哪一行**，就重点查该阶段（不要先怀疑业务逻辑）。

### 常见根因与修复

| 根因 | 说明 | 修复 |
|------|------|------|
| **I2C0 软件路径访问硬件寄存器** | `bsp_i2c_init`/`gpio_scan_begin` 在未 `SysCtlPeripheralEnable(I2C0)` 时调用 `I2CMasterDisable` → **卡死在 periph init** | I2C0 软件 I2C **禁止**读写 `I2CMaster*`；仅 `GPIOPinTypeGPIOOutputOD` + 位操作 |
| **软件/硬件 I2C 混用** | 扫描用 GPIO 位操作，读写却切 `GPIOPinTypeI2C` + 硬件主机 | I2C0 统一走 `bsp_i2c` 软件路径；`gen_config.py` 对 I2C0 不生成硬件引脚复用 |
| **GPIO 扫描后总线未恢复** | SCL/SDA 停在 GPIO 输入/硬件复用异常 | `bsp_i2c_init(I2C0)` → `bsp_i2c0_gpio_scan_begin()`；读写前勿 `i2c0_restore_pin_mux` |
| **厂测过早初始化电机/QEI** | `Motor_Init`/`Encoder_Init` 在传感器厂测前拉起 Timer/QEI，偶发阻塞启动 | 厂测 `Factory_Board_Init` 仅 `Board_Periph_Init`，电机项测再 init |
| **`event_schedule` 无首事件** | `factory_evt` 进循环后 `event_schedule` 永久等信号量 | 进循环前 `event_set(EVT_ID_TIMER)`；确认 `factory_tmr` 已创建 |
| **堆栈/堆耗尽** | 任务创建失败或栈溢出 | 看 `[rtos] malloc failed` / `stack overflow`；加大 `configTOTAL_HEAP_SIZE` 或 `FACTORY_EVT_STACK_WORDS` |
| **调度器前误用阻塞 RTOS API** | `vTaskDelay`/`xSemaphoreTake(portMAX_DELAY)` 在 `vTaskStartScheduler` 前调用 | 调度器前仅用 `bsp_delay_us/ms`（DWT）；RTOS 同步在任务内 |

### 修改 I2C/厂测代码时的检查清单

1. I2C0 扫描、IMU、磁力计是否**同一条**软件 I2C 路径？
2. 是否误调 `bsp_i2c_bus_release(..., reinit_master)` 把 I2C0 切回硬件主机？
3. `Board_Periph_Init` 后是否仍打印 `[factory] board ok`？
4. 重新烧录的是 `projects/car-4wd/build/factory.bin`（非旧缓存）？
5. 传感器地址是否与扫描一致：**MPU6050=0x69**、**QMC5883P=0x2C**（暂无 OLED）？

### 相关文件

- 厂测入口：`factory/main.c`、`factory/factory.c`
- I2C BSP：`bsp_driver/src/bsp_i2c.c`
- 板级生成：`scripts/gen_config.py`（I2C0 跳过硬件 mux）
- 传感器：`Common/src/imu.c`、`Common/src/magnetometer.c`

## 文档索引

| 文档 | 内容 |
|------|------|
| `docs/build.md` | 编译、构建、烧录、脚本 |
| `docs/project-overview.md` | 项目总览 |
| `docs/syscfg-io-allocation.md` | 双车型引脚分配 |
| `docs/resource-allocation.md` | 外设/定时器/DMA/中断规划 |
| `docs/car-chassis-reference.md` | 底盘开发参考与 TivaWare API 示例 |
| `docs/chip-selection.md` | MCU 选型分析 |
| `docs/peripheral-selection.md` | 外设模块选型（TB6612、MPU6050 等） |
| `docs/embedded-coding-standard.md` | 依赖分层与目录规范 |

